# Experiment 8: Type Checking

Implements basic semantic type checking — verifies operand type compatibility for expressions (e.g. int vs float) and flags type mismatches.

## Build & Run
```
gcc typecheck.c -o typecheck
./typecheck
```
