# Experiment 10: Backend Compiler for 8086

Generates simple 8086 assembly instructions from intermediate three-address code, mapping arithmetic operations onto 8086 register-based instructions (MOV, ADD, SUB, MUL etc).

## Build & Run
```
gcc backend.c -o backend
./backend
```
