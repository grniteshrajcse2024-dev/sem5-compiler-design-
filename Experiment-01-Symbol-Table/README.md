# Experiment 1: Symbol Table Construction

Implements a basic symbol table using a hash table / array of structs to store identifier name, type, and scope info as encountered during a single pass over source code.

## Build & Run
```
gcc symbol_table.c -o symtab
./symtab
```
