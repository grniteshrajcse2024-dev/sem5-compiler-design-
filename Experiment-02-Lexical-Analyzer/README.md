# Experiment 2: Lexical Analyzer

A hand-written lexical analyzer in C that scans source text character by character and classifies tokens into categories (keywords, identifiers, operators, numbers, etc.) without using Lex/Flex.

## Build & Run
```
gcc lexer.c -o lexer
./lexer
```
